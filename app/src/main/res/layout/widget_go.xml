<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="4dp">

    <!-- Clickable pill; its background tint carries the state colour -->
    <ImageButton
        android:id="@+id/widget_button"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/widget_bg"
        android:contentDescription="LazyCar GO" />

    <!-- Car / stop glyph -->
    <ImageView
        android:id="@+id/widget_glyph"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="14dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_car_glyph" />

    <!-- Busy spinner, shown only while starting / stopping -->
    <ProgressBar
        android:id="@+id/widget_progress"
        style="?android:attr/progressBarStyle"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:indeterminateTint="#FFFFFFFF"
        android:visibility="gone" />
</FrameLayout>
